[downloads]
# Folder where original files will be downloaded
root_folder = "./downloads"
# Folder where final FLAC files will be exported
flac_folder = "./downloads/flac"
# Folder where final MP3 files will be exported
mp3_folder = "./downloads/mp3"
# Folder where final WAV files will be exported
wav_folder = "./downloads/wav"

[metadata]
# Relevant remix tokens (everything else will be removed from the title)
supported_remix_tokens = ["remix", "bootleg", "flip", "edit"]
# Delimiter used to separate contributing artists tag
artist_delimiter = "; "
# Indicates whether or not to use the title of a single as its album or leave the tag blank
tag_single_album = false
# Default genre to use when tagging tracks
default_genre = "Dubstep"

[soundcloud]
# Website names that will be detected as a download gate
supported_download_gates = ["hypeddit", "toneden"]
# SoundCloud playlist to download (private or public)
download_playlist_url = ""

[spotify]
# Client ID of the registered Spotify application
client_id = ""
# Client secret of the registered Spotify application
client_secret = ""
# Redirect URI used by Spotify OAuth
redirect_url = "https://localhost:8888/callback"

[deemix]
# Download quality (0 = MP3@128, 1 = MP3@320, 2 = FLAC@44.1)
default_quality = 1
# Premium Deezer ARL (required)
# See https://rentry.org/firehawk52#deezer-arls
deezer_arl = ""
